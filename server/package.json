{
  "name": "pulse-talk-server",
  "version": "1.1.0",
  "description": "Il piano di controllo di PulseTalk: chi entra, in quale stanza, con quali permessi.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "pulse-talk": "src/cli.mjs"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "test": "node --test \"test/*.test.mjs\"",
    "invita": "node src/cli.mjs invita"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/static": "^8.1.3",
    "better-sqlite3": "^12.2.0",
    "fastify": "^5.2.0",
    "livekit-server-sdk": "^2.17.0"
  }
}
